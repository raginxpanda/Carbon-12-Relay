RELAY 0.2.3 — fixes catch-up "found=1710 sent=0"
================================================
TWO bugs fixed:
  1) DEDUPE: logs echo each blueprint many times + across 173 files -> that's why
     "found" was 1710. Now deduped to unique blueprint names before sending.
  2) DRAIN: old catch-up sent only ONE batch of 100 then stopped. Now it loops
     until the whole queue is sent (drainAll).
  Plus: catch-up now shows a SEND ERROR reason if the bot rejects a batch.

APPLY (drag & drop)
  1. Close the relay (tray -> Quit).
  2. Drag "src" and "electron" into your project folder -> Replace -> Yes.
  3. Ship:  npm version patch   then   git push --follow-tags   (publish the draft release)
     or test now:  npm run dist  -> install dist\...Setup.exe

THEN hit Catch up and read the line:
  - "Synced N blueprint(s)"  + sent>0        -> WORKING. Check /blueprint mine.
  - "Nothing synced ... SEND ERROR: <reason>" -> tell me the reason:
        unauthorized  -> regenerate token in quarters (yours was shared in chat anyway)
        http 500      -> bot error on ingest -> I fix the bot
        http 413      -> payload too big -> lower batch (tell me)
        network       -> endpoint/connectivity

Note: after this, /blueprint mine should fill. The bot dedupes on its side too,
so re-running catch-up is safe (no doubles).
