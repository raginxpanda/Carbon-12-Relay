RELAY 0.2.22 — blueprint catalog: search + sort
================================================
Only electron/index.html changed. Drag in -> Replace -> Yes. Consolidated set
(carries all prior electron work).

TWO REQUESTED ADDITIONS to "Browse Full Blueprint Catalog":

1) SEARCH — a search box at the top of the catalog. Type any part of a blueprint
   name and the list filters live as you type. Works together with the category
   tabs (search within a category, or across "All"). Auto-focuses when the catalog
   opens so you can just start typing.

2) SORT — a dropdown with four options:
     • Newest first  (default — most recently collected on top)
     • Oldest first  (earliest collected first; blueprints with no recorded date
                      sort last, since their age is unknown)
     • Name A–Z
     • Name Z–A

   Search, sort, and the category tabs all compose — pick a category, type a
   search, choose a sort, and they stack correctly. The count in the header shows
   "N shown" when filtered, "N owned" otherwise.

NO bot change. SHIP: npm version patch ; git push --follow-tags
