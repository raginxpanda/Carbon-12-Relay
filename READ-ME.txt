RELAY 0.2.10 — logo + catalog-close fix + collapsible orgs
==========================================================
Only electron/index.html changed. Drag it into your project -> Replace -> Yes.

FIXES
 1) Real Carbon-12 logo in the header (was the "C12" text badge). Embedded, no
    extra file needed.
 2) BUG FIX: the blueprint catalog X now closes it (before, the close button did
    nothing because it loaded after the script — now handled by delegation).
    Also: click the dark backdrop OR press Esc to close.
 3) "Paired orgs" is now a collapsible dropdown — auto-open when you have no org,
    auto-collapsed once you're paired (shows a count, click to expand/manage).

SHIP: npm version patch ; git push --follow-tags   (or npm run dist to test)
